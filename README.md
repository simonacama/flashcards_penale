# La palestra di Momi per diritto penale

Webapp React/Vite mobile-first per studio di diritto penale.

## Sviluppo

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

La configurazione Vite usa `base: "/flashcards_penale/"`, adatta alla pubblicazione del repository su GitHub Pages.
